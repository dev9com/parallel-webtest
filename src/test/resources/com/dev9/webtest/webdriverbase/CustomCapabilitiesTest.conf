parameters {
    globalCustomCapabilities {
        name:           veryCustomName
        fakeCapability: "What happens if I enter a bad capability?"
        custom-data {
            release:    experimental
        }
    }
}